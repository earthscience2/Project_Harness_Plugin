**Checkbox / Radio** — labelled selection controls. `Checkbox` for independent toggles, `Radio` for one-of-many (share a `name`).

```jsx
<Checkbox label="실패한 케이스만 표시" defaultChecked />
<Radio name="env" label="staging" defaultChecked />
<Radio name="env" label="production" />
```
