# 가설/질문

알고 싶은 것의 목록. 각 항목은 검증 가능하게.

| id | 가설/질문 | status | 검증한 실험(exp) | 메모 |
|----|----------|--------|-----------------|------|
| h1 | TODO | open | — | |

- `status`: `open`(미착수) · `testing`(런 돌리는 중) · `confirmed` · `refuted`
- 확정/반증되면 근거 `run`/`exp` id 를 적고 → `인사이트` 영역으로 승격.
